import React from "react";

const page = () => {
	return (
		<>
			<div className="max-w-240 mx-auto mt-12 px-4">
				<h1 className="text-2xl font-black">Settings</h1>
				<div className="flex gap-4 flex-wrap">
					{/* Loop through the workspaces that the user has access to */}
				</div>
			</div>
		</>
	);
};

export default page;
